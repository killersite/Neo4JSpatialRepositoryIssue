Demonstration of issues with doing a 'mvn package' seem to be the interaction between @ClassBridge and the @NodeEntity.

On a build with "clean" I get the aspectj error and subsequent builds without "clean" the error is gone.

